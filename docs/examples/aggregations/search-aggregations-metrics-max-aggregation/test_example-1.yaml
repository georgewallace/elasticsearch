# Source: aggregations/search-aggregations-metrics-max-aggregation.md
--- !metadata
id: example-1
language: elastic_console
topic: search-aggregations-metrics-max-aggregation
subtopic: default
example_order: 1
--- !code
|
  POST /sales/_search?size=0
  {
    "aggs": {
      "max_price": { "max": { "field": "price" } }
    }
  }
--- !result
|
  {
    ...
    "aggregations": {
        "max_price": {
            "value": 200.0
        }
    }
  }
--- !test
stage: sales
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
