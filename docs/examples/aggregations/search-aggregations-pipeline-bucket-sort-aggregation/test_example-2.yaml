# Source: aggregations/search-aggregations-pipeline-bucket-sort-aggregation.md
--- !metadata
id: example-2
language: elastic_console
topic: search-aggregations-pipeline-bucket-sort-aggregation
subtopic: default
example_order: 2
--- !code
|
  POST /sales/_search
  {
    "size": 0,
    "aggs": {
      "sales_per_month": {
        "date_histogram": {
          "field": "date",
          "calendar_interval": "month"
        },
        "aggs": {
          "bucket_truncate": {
            "bucket_sort": {
              "from": 1,
              "size": 1
            }
          }
        }
      }
    }
  }
--- !result
|
  {
     "took": 11,
     "timed_out": false,
     "_shards": ...,
     "hits": ...,
     "aggregations": {
        "sales_per_month": {
           "buckets": ...
        }
     }
  }
--- !test
stage: sales
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
