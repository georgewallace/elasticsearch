# Source: search-aggregations-metrics-percentile-aggregation.md
--- !metadata
id: example-6
language: elastic_console
topic: search-aggregations-metrics-percentile-aggregation
subtopic: default
example_order: 6
--- !code
|
  GET latency/_search
  {
    "size": 0,
    "aggs": {
      "load_time_outlier": {
        "percentiles": {
          "field": "load_time",
          "tdigest": {
            "execution_hint": "high_accuracy"    
          }
        }
      }
    }
  }
--- !result

--- !test
stage: latency
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
