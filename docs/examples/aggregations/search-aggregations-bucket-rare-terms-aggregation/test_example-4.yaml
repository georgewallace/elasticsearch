# Source: search-aggregations-bucket-rare-terms-aggregation.md
--- !metadata
id: example-4
language: elastic_console
topic: search-aggregations-bucket-rare-terms-aggregation
subtopic: default
example_order: 4
--- !code
|
  GET /_search
  {
    "aggs": {
      "genres": {
        "rare_terms": {
          "field": "genre",
          "include": [ "swing", "rock" ],
          "exclude": [ "jazz" ]
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
