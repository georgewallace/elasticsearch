# Source: pipeline.md
--- !metadata
id: example-1
language: elastic_console
topic: pipeline
subtopic: default
example_order: 1
--- !code
|
  POST /_search
  {
    "aggs": {
      "my_date_histo": {
        "date_histogram": {
          "field": "timestamp",
          "calendar_interval": "day"
        },
        "aggs": {
          "the_sum": {
            "sum": { "field": "lemmings" }              
          },
          "the_deriv": {
            "derivative": { "buckets_path": "the_sum" } 
          }
        }
      }
    }
  }
--- !result

--- !test
stage: sales
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
