# Source: aggregations/pipeline.md
--- !metadata
id: example-5
language: elastic_console
topic: pipeline
subtopic: default
example_order: 5
--- !code
|
  POST /sales/_search
  {
    "size": 0,
    "aggs": {
      "histo": {
        "date_histogram": {
          "field": "date",
          "calendar_interval": "day"
        },
        "aggs": {
          "categories": {
            "terms": {
              "field": "category"
            }
          },
          "min_bucket_selector": {
            "bucket_selector": {
              "buckets_path": {
                "count": "categories._bucket_count" 
              },
              "script": {
                "source": "params.count != 0"
              }
            }
          }
        }
      }
    }
  }
--- !result

--- !test
stage: sales
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      ignore_fields:
        - took
