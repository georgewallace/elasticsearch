# Source: search-aggregations-metrics-percentile-rank-aggregation.md
--- !metadata
id: example-1
language: elastic_console
topic: search-aggregations-metrics-percentile-rank-aggregation
subtopic: default
example_order: 1
--- !code
|
  GET latency/_search
  {
    "size": 0,
    "aggs": {
      "load_time_ranks": {
        "percentile_ranks": {
          "field": "load_time",   
          "values": [ 500, 600 ]
        }
      }
    }
  }
--- !result
|
  {
    ...

   "aggregations": {
      "load_time_ranks": {
        "values": {
          "500.0": 90.01,
          "600.0": 100.0
        }
      }
    }
  }
--- !test
stage: latency
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
