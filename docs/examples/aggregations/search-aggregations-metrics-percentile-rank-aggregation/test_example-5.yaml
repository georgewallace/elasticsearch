# Source: aggregations/search-aggregations-metrics-percentile-rank-aggregation.md
--- !metadata
id: example-5
language: elastic_console
topic: search-aggregations-metrics-percentile-rank-aggregation
subtopic: default
example_order: 5
--- !code
|
  GET latency/_search
  {
    "size": 0,
    "aggs": {
      "load_time_ranks": {
        "percentile_ranks": {
          "field": "load_time",
          "values": [ 500, 600 ],
          "missing": 10           
        }
      }
    }
  }
--- !result

--- !test
stage: latency
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
