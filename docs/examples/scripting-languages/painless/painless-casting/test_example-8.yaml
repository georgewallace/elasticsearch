# Source: scripting-languages/painless/painless-casting.md
--- !metadata
id: example-8
language: painless
topic: painless-casting
subtopic: default
example_order: 8
--- !code
|
  def d = 1;                  
  short s = d;       // error 
  d = new HashMap();          
  List l = d;        // error
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
