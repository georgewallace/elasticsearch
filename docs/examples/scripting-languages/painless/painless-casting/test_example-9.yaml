# Source: scripting-languages/painless/painless-casting.md
--- !metadata
id: example-9
language: painless
topic: painless-casting
subtopic: default
example_order: 9
--- !code
|
  char c = (char)"C"; 
  c = (char)'c';
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
