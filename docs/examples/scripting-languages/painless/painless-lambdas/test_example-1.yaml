# Source: scripting-languages/painless/painless-lambdas.md
--- !metadata
id: example-1
language: painless
topic: painless-lambdas
subtopic: default
example_order: 1
--- !code
|
  list.removeIf(item -> item == 2);
  list.removeIf((int item) -> item == 2);
  list.removeIf((int item) -> { item == 2 });
  list.sort((x, y) -> x - y);
  list.sort(Integer::compare);
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
