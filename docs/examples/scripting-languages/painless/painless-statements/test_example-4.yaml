# Source: scripting-languages/painless/painless-statements.md
--- !metadata
id: example-4
language: painless
topic: painless-statements
subtopic: default
example_order: 4
--- !code
|
  while (ctx._source.item < condition) {
    // do something
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
