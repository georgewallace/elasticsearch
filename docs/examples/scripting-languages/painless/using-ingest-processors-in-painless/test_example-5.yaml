# Source: scripting-languages/painless/using-ingest-processors-in-painless.md
--- !metadata
id: example-5
language: painless
topic: using-ingest-processors-in-painless
subtopic: default
example_order: 5
--- !code
|
  void json(Map<String, Object> map, String key);
  Object json(Object value);
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
