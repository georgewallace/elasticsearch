# Source: scripting-languages/painless/painless-runtime-fields-context.md
--- !metadata
id: example-1
language: elastic_console
topic: painless-runtime-fields-context
subtopic: default
example_order: 1
--- !code
|
  PUT seats/_mapping
  {
    "runtime": {
      "day_of_week": {
        "type": "keyword",
        "script": {
          "source": "emit(doc['datetime'].value.getDayOfWeekEnum().toString())"
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
