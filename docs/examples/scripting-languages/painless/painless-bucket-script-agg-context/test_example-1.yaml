# Source: scripting-languages/painless/painless-bucket-script-agg-context.md
--- !metadata
id: example-1
language: painless
topic: painless-bucket-script-agg-context
subtopic: default
example_order: 1
--- !code
|
  (params.max - params.min) + params.base_cost
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
