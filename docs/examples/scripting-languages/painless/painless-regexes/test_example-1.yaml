# Source: scripting-languages/painless/painless-regexes.md
--- !metadata
id: example-1
language: painless
topic: painless-regexes
subtopic: default
example_order: 1
--- !code
|
  Pattern p = /[aeiou]/
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
