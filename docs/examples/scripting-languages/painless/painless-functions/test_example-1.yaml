# Source: scripting-languages/painless/painless-functions.md
--- !metadata
id: example-1
language: painless
topic: painless-functions
subtopic: default
example_order: 1
--- !code
|
  boolean isNegative(def x) { x < 0 }
  ...
  if (isNegative(someVar)) {
    ...
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
