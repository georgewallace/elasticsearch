# Source: scripting-languages/painless/painless-operators-numeric.md
--- !metadata
id: example-8
language: painless
topic: painless-operators-numeric
subtopic: default
example_order: 8
--- !code
|
  def x = 1; 
  --x;
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
