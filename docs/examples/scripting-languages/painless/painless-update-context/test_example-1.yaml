# Source: scripting-languages/painless/painless-update-context.md
--- !metadata
id: example-1
language: elastic_console
topic: painless-update-context
subtopic: default
example_order: 1
--- !code
|
  POST /seats/_update/3
  {
    "script": {
      "source": "ctx._source.sold = true; ctx._source.cost = params.sold_cost",
      "lang": "painless",
      "params": {
        "sold_cost": 26
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
