# Source: elasticsearch-plugins/repository-hdfs-security.md
--- !metadata
id: example-1
language: elastic_console
topic: repository-hdfs-security
subtopic: default
example_order: 1
--- !code
|
  PUT _snapshot/my_hdfs_repository
  {
    "type": "hdfs",
    "settings": {
      "uri": "hdfs://namenode:8020/",
      "path": "/user/elasticsearch/repositories/my_hdfs_repository",
      "security.principal": "elasticsearch@REALM"
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
