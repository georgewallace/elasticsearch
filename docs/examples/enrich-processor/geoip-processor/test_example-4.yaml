# Source: enrich-processor/geoip-processor.md
--- !metadata
id: example-4
language: elastic_console
topic: geoip-processor
subtopic: default
example_order: 4
--- !code
|
  PUT my_ip_locations
  {
    "mappings": {
      "properties": {
        "geoip": {
          "properties": {
            "location": { "type": "geo_point" }
          }
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
