# Source: enrich-processor/pipeline-processor.md
--- !metadata
id: example-2
language: elastic_console
topic: pipeline-processor
subtopic: default
example_order: 2
--- !code
|
  PUT _ingest/pipeline/pipelineB
  {
    "description" : "outer pipeline",
    "processors" : [
      {
        "pipeline" : {
          "name": "pipelineA"
        }
      },
      {
        "set" : {
          "field": "outer_pipeline_set",
          "value": "outer"
        }
      }
    ]
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
