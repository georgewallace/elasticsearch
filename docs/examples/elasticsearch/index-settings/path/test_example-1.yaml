# Source: elasticsearch/index-settings/path.md
--- !metadata
id: example-1
language: elastic_console
topic: path
subtopic: default
example_order: 1
--- !code
|
  PUT _cluster/settings
  {
    "persistent": {
      "cluster.routing.allocation.exclude._name": "target-node-name"
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
