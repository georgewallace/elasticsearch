# Source: elasticsearch/index-settings/sorting.md
--- !metadata
id: example-4
language: elastic_console
topic: sorting
subtopic: default
example_order: 4
--- !code
|
  GET /events/_search
  {
    "size": 10,
    "sort": [
      { "timestamp": "desc" }
    ]
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
