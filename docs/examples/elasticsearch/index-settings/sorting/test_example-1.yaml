# Source: elasticsearch/index-settings/sorting.md
--- !metadata
id: example-1
language: elastic_console
topic: sorting
subtopic: default
example_order: 1
--- !code
|
  PUT my-index-000001
  {
    "settings": {
      "index": {
        "sort.field": "date", 
        "sort.order": "desc"  
      }
    },
    "mappings": {
      "properties": {
        "date": {
          "type": "date"
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
