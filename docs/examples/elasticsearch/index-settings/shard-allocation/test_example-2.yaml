# Source: elasticsearch/index-settings/shard-allocation.md
--- !metadata
id: example-2
language: elastic_console
topic: shard-allocation
subtopic: default
example_order: 2
--- !code
|
  PUT test/_settings
  {
    "index.routing.allocation.require.size": "big",
    "index.routing.allocation.require.rack": "rack1"
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
