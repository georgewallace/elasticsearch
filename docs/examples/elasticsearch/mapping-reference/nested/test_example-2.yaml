# Source: elasticsearch/mapping-reference/nested.md
--- !metadata
id: example-2
language: elastic_console
topic: nested
subtopic: default
example_order: 2
--- !code
|
  GET my-index-000001/_search
  {
    "query": {
      "bool": {
        "must": [
          { "match": { "user.first": "Alice" }},
          { "match": { "user.last":  "Smith" }}
        ]
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
