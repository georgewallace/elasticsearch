# Source: elasticsearch/mapping-reference/rank-vectors.md
--- !metadata
id: example-7
language: elastic_console
topic: rank-vectors
subtopic: default
example_order: 7
--- !code
|
  PUT my-index-include-vectors
  {
    "settings": {
      "index.mapping.exclude_source_vectors": false
    },
    "mappings": {
      "properties": {
        "my_vector": {
          "type": "rank_vectors",
          "dims": 128
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
