# Source: elasticsearch/mapping-reference/match-only-text.md
--- !metadata
id: example-1
language: elastic_console
topic: match-only-text
subtopic: default
example_order: 1
--- !code
|
  PUT logs
  {
    "mappings": {
      "properties": {
        "@timestamp": {
          "type": "date"
        },
        "message": {
          "type": "match_only_text"
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
