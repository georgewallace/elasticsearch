# Source: elasticsearch/mapping-reference/sparse-vector.md
--- !metadata
id: example-1
language: elastic_console
topic: sparse-vector
subtopic: default
example_order: 1
--- !code
|
  PUT my-index
  {
    "mappings": {
      "properties": {
        "text.tokens": {
          "type": "sparse_vector"
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
