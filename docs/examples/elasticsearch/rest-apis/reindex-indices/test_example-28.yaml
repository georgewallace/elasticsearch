# Source: elasticsearch/rest-apis/reindex-indices.md
--- !metadata
id: example-28
language: elastic_console
topic: reindex-indices
subtopic: default
example_order: 28
--- !code
|
  GET _tasks?actions=*reindex
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
