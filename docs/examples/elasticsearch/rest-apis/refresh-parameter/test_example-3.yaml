# Source: elasticsearch/rest-apis/refresh-parameter.md
--- !metadata
id: example-3
language: elastic_console
topic: refresh-parameter
subtopic: default
example_order: 3
--- !code
|
  PUT /test/_doc/4?refresh=wait_for
  {"test": "test"}
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
