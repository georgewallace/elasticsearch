# Source: elasticsearch/rest-apis/search-suggesters.md
--- !metadata
id: example-21
language: elastic_console
topic: search-suggesters
subtopic: default
example_order: 21
--- !code
|
  POST place/_search?pretty
  {
    "suggest": {
      "place_suggestion": {
        "prefix": "tim",
        "completion": {
          "field": "suggest",
          "size": 10,
          "contexts": {
            "place_type": [                             
              { "context": "cafe" },
              { "context": "restaurants", "boost": 2 }
            ]
          }
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
