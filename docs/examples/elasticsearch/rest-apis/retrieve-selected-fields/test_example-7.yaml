# Source: elasticsearch/rest-apis/retrieve-selected-fields.md
--- !metadata
id: example-7
language: elastic_console
topic: retrieve-selected-fields
subtopic: default
example_order: 7
--- !code
|
  GET /_search
  {
    "_source": "obj.*",
    "query": {
      "match": {
        "user.id": "kimchy"
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
