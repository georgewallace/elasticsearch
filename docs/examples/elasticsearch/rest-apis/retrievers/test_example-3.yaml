# Source: elasticsearch/rest-apis/retrievers.md
--- !metadata
id: example-3
language: elastic_console
topic: retrievers
subtopic: default
example_order: 3
--- !code
|
  GET books/_search
  {
    "retriever": {
      "linear": {
        "query": "elasticsearch",
        "fields": [
          "title",
          "description",
          "title_semantic",
          "description_semantic"
        ],
        "normalizer": "minmax"
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
