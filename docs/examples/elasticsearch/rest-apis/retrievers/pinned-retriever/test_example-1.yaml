# Source: elasticsearch/rest-apis/retrievers/pinned-retriever.md
--- !metadata
id: example-1
language: elastic_console
topic: pinned-retriever
subtopic: default
example_order: 1
--- !code
|
  GET /restaurants/_search
  {
    "retriever": {
      "pinned": {
        "docs": [
          { "_id": "doc1", "_index": "my-index" },
          { "_id": "doc2" }
        ],
        "retriever": {
          "standard": {
            "query": {
              "match": {
                "title": "elasticsearch"
              }
            }
          }
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
