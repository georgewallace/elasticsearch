# Source: elasticsearch/rest-apis/retrievers/knn-retriever.md
--- !metadata
id: example-1
language: elastic_console
topic: knn-retriever
subtopic: default
example_order: 1
--- !code
|
  GET /restaurants/_search
  {
    "retriever": {
      "knn": { 
        "field": "vector", 
        "query_vector": [10, 22, 77], 
        "k": 10, 
        "num_candidates": 10 
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
