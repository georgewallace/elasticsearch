# Source: elasticsearch/rest-apis/search-shard-routing.md
--- !metadata
id: example-6
language: elastic_console
topic: search-shard-routing
subtopic: default
example_order: 6
--- !code
|
  GET /my-index-000001/_search?max_concurrent_shard_requests=3
  {
    "query": {
      "match": {
        "user.id": "kimchy"
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
