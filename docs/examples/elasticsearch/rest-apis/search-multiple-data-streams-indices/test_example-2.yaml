# Source: elasticsearch/rest-apis/search-multiple-data-streams-indices.md
--- !metadata
id: example-2
language: elastic_console
topic: search-multiple-data-streams-indices
subtopic: default
example_order: 2
--- !code
|
  GET /my-index-*/_search
  {
    "query": {
      "match": {
        "user.id": "kimchy"
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
