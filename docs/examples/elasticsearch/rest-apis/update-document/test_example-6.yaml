# Source: elasticsearch/rest-apis/update-document.md
--- !metadata
id: example-6
language: elastic_console
topic: update-document
subtopic: default
example_order: 6
--- !code
|
  POST test/_update/1
  {
    "script" : "ctx._source.remove('new_field')"
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
