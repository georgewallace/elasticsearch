# Source: elasticsearch/rest-apis/search-rank-eval.md
--- !metadata
id: example-4
language: elastic_console
topic: search-rank-eval
subtopic: default
example_order: 4
--- !code
|
  GET /my-index-000001/_rank_eval
  {
    "requests": [
      {
        "id": "JFK query",
        "request": { "query": { "match_all": {} } },
        "ratings": []
      } ],
    "metric": {
      "dcg": {
        "k": 20,
        "normalize": false
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
