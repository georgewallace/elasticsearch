# Source: elasticsearch/rest-apis/filter-search-results.md
--- !metadata
id: example-2
language: elastic_console
topic: filter-search-results
subtopic: default
example_order: 2
--- !code
|
  GET /shirts/_search
  {
    "query": {
      "bool": {
        "filter": [
          { "term": { "color": "red"   }},
          { "term": { "brand": "gucci" }}
        ]
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
