# Source: elasticsearch/rest-apis/shard-request-cache.md
--- !metadata
id: example-4
language: elastic_console
topic: shard-request-cache
subtopic: default
example_order: 4
--- !code
|
  GET /my-index-000001/_search?request_cache=true
  {
    "size": 0,
    "aggs": {
      "popular_colors": {
        "terms": {
          "field": "colors"
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
