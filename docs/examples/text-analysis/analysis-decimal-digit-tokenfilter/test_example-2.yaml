# Source: text-analysis/analysis-decimal-digit-tokenfilter.md
--- !metadata
id: example-2
language: elastic_console
topic: analysis-decimal-digit-tokenfilter
subtopic: default
example_order: 2
--- !code
|
  PUT /decimal_digit_example
  {
    "settings": {
      "analysis": {
        "analyzer": {
          "whitespace_decimal_digit": {
            "tokenizer": "whitespace",
            "filter": [ "decimal_digit" ]
          }
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
