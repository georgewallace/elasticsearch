# Source: text-analysis/analysis-kstem-tokenfilter.md
--- !metadata
id: example-1
language: elastic_console
topic: analysis-kstem-tokenfilter
subtopic: default
example_order: 1
--- !code
|
  GET /_analyze
  {
    "tokenizer": "standard",
    "filter": [ "kstem" ],
    "text": "the foxes jumping quickly"
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
