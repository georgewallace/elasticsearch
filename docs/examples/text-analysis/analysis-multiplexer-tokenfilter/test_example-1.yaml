# Source: text-analysis/analysis-multiplexer-tokenfilter.md
--- !metadata
id: example-1
language: elastic_console
topic: analysis-multiplexer-tokenfilter
subtopic: default
example_order: 1
--- !code
|
  PUT /multiplexer_example
  {
    "settings": {
      "analysis": {
        "analyzer": {
          "my_analyzer": {
            "tokenizer": "standard",
            "filter": [ "my_multiplexer" ]
          }
        },
        "filter": {
          "my_multiplexer": {
            "type": "multiplexer",
            "filters": [ "lowercase", "lowercase, porter_stem" ]
          }
        }
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
