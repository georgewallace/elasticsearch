# Source: text-analysis/analysis-classic-tokenizer.md
--- !metadata
id: example-1
language: elastic_console
topic: analysis-classic-tokenizer
subtopic: default
example_order: 1
--- !code
|
  POST _analyze
  {
    "tokenizer": "classic",
    "text": "The 2 QUICK Brown-Foxes jumped over the lazy dog's bone."
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
