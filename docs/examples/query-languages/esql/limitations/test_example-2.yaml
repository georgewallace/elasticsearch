# Source: query-languages/esql/limitations.md
--- !metadata
id: example-2
language: esql
topic: limitations
subtopic: default
example_order: 2
--- !code
|
  FROM airports
  | EVAL geohex = ST_GEOHEX(location, 1)
  | EVAL x = ST_X(location), y = ST_Y(location)
  | KEEP x, y, geohex
--- !result

--- !test
stage: airports
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
