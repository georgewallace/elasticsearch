# Source: query-languages/esql/esql-getting-started.md
--- !metadata
id: example-13
language: esql
topic: esql-getting-started
subtopic: default
example_order: 13
--- !code
|
  FROM sample_data
  | STATS median_duration = MEDIAN(event_duration), max_duration = MAX(event_duration)
--- !result

--- !test
stage: sample_data
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
