# Source: query-languages/esql/_snippets/operators/detailedDescription/rlike.md
--- !metadata
id: example-3
language: esql
topic: rlike
subtopic: default
example_order: 3
--- !code
|
  ROW message = "foobar"
  | WHERE message RLIKE ("foo.*", "bar.")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
