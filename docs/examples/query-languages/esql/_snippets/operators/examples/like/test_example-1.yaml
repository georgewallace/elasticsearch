# Source: query-languages/esql/_snippets/operators/examples/like.md
--- !metadata
id: example-1
language: esql
topic: like
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | WHERE first_name LIKE """?b*"""
  | KEEP first_name, last_name
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
