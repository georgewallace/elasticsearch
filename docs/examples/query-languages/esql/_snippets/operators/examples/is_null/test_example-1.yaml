# Source: query-languages/esql/_snippets/operators/examples/is_null.md
--- !metadata
id: example-1
language: esql
topic: is_null
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | WHERE birth_date IS NULL
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
