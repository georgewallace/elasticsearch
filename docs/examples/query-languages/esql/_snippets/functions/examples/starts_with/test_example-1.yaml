# Source: query-languages/esql/_snippets/functions/examples/starts_with.md
--- !metadata
id: example-1
language: esql
topic: starts_with
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP last_name
  | EVAL ln_S = STARTS_WITH(last_name, "B")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
