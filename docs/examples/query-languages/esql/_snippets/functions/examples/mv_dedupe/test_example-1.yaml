# Source: query-languages/esql/_snippets/functions/examples/mv_dedupe.md
--- !metadata
id: example-1
language: esql
topic: mv_dedupe
subtopic: default
example_order: 1
--- !code
|
  ROW a=["foo", "foo", "bar", "foo"]
  | EVAL dedupe_a = MV_DEDUPE(a)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
