# Source: query-languages/esql/_snippets/functions/examples/term.md
--- !metadata
id: example-1
language: esql
topic: term
subtopic: default
example_order: 1
--- !code
|
  FROM books
  | WHERE TERM(author, "gabriel")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
