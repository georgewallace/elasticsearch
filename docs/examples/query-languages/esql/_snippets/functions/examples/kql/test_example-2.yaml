# Source: query-languages/esql/_snippets/functions/examples/kql.md
--- !metadata
id: example-2
language: esql
topic: kql
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | WHERE KQL("mary", {"case_insensitive": true, "default_field": "first_name", "boost": 1.5})
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
