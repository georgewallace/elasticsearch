# Source: query-languages/esql/_snippets/functions/examples/match.md
--- !metadata
id: example-2
language: esql
topic: match
subtopic: default
example_order: 2
--- !code
|
  FROM books
  | WHERE MATCH(title, "Hobbit Back Again", {"operator": "AND"})
  | KEEP title;
--- !result

--- !test
stage: books
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
