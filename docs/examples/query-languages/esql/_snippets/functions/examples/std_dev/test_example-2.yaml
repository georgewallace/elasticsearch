# Source: query-languages/esql/_snippets/functions/examples/std_dev.md
--- !metadata
id: example-2
language: esql
topic: std_dev
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS stddev_salary_change = STD_DEV(MV_MAX(salary_change))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
