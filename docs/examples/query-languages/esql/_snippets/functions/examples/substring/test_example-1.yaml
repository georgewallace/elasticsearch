# Source: query-languages/esql/_snippets/functions/examples/substring.md
--- !metadata
id: example-1
language: esql
topic: substring
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP last_name
  | EVAL ln_sub = SUBSTRING(last_name, 1, 3)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
