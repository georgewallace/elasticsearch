# Source: query-languages/esql/_snippets/functions/examples/date_trunc.md
--- !metadata
id: example-2
language: esql
topic: date_trunc
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | EVAL year = DATE_TRUNC(1 year, hire_date)
  | STATS hires = COUNT(emp_no) BY year
  | SORT year
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
