# Source: query-languages/esql/_snippets/functions/examples/v_cosine.md
--- !metadata
id: example-1
language: esql
topic: v_cosine
subtopic: default
example_order: 1
--- !code
|
  from colors
  | where color != "black"
  | eval similarity = v_cosine(rgb_vector, [0, 255, 255])
  | sort similarity desc, color asc
--- !result

--- !test
stage: colors
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
