# Source: query-languages/esql/_snippets/functions/examples/count_distinct.md
--- !metadata
id: example-3
language: esql
topic: count_distinct
subtopic: default
example_order: 3
--- !code
|
  ROW words="foo;bar;baz;qux;quux;foo"
  | STATS distinct_word_count = COUNT_DISTINCT(SPLIT(words, ";"))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
