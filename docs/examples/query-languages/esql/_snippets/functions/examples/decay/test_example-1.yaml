# Source: query-languages/esql/_snippets/functions/examples/decay.md
--- !metadata
id: example-1
language: esql
topic: decay
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | EVAL decay_result = decay(salary, 0, 100000, {"offset": 5, "decay": 0.5, "type": "linear"})
  | SORT decay_result DESC
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
