# Source: query-languages/esql/_snippets/functions/examples/delta.md
--- !metadata
id: example-1
language: esql
topic: delta
subtopic: default
example_order: 1
--- !code
|
  TS k8s
  | WHERE pod == "one"
  | STATS tx = SUM(DELTA(network.bytes_in)) BY cluster, time_bucket = TBUCKET(10minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
