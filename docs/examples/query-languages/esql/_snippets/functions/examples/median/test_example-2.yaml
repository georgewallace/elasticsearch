# Source: query-languages/esql/_snippets/functions/examples/median.md
--- !metadata
id: example-2
language: esql
topic: median
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS median_max_salary_change = MEDIAN(MV_MAX(salary_change))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
