# Source: query-languages/esql/_snippets/functions/examples/mv_pseries_weighted_sum.md
--- !metadata
id: example-1
language: esql
topic: mv_pseries_weighted_sum
subtopic: default
example_order: 1
--- !code
|
  ROW a = [70.0, 45.0, 21.0, 21.0, 21.0]
  | EVAL sum = MV_PSERIES_WEIGHTED_SUM(a, 1.5)
  | KEEP sum
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
