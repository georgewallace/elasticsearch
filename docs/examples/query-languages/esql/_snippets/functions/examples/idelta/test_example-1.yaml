# Source: query-languages/esql/_snippets/functions/examples/idelta.md
--- !metadata
id: example-1
language: esql
topic: idelta
subtopic: default
example_order: 1
--- !code
|
  TS k8s
  | STATS events = SUM(IDELTA(events_received)) by pod, time_bucket = TBUCKET(10minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
