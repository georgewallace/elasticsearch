# Source: query-languages/esql/_snippets/functions/examples/tbucket.md
--- !metadata
id: example-2
language: esql
topic: tbucket
subtopic: default
example_order: 2
--- !code
|
  FROM sample_data
  | STATS min = MIN(@timestamp), max = MAX(@timestamp) BY bucket = TBUCKET("1 hour")
  | SORT min
--- !result

--- !test
stage: sample_data
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
