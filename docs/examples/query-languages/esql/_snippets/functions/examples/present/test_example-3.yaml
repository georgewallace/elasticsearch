# Source: query-languages/esql/_snippets/functions/examples/present.md
--- !metadata
id: example-3
language: esql
topic: present
subtopic: default
example_order: 3
--- !code
|
  FROM employees
  | WHERE emp_no == 10020
  | STATS is_present = TO_INTEGER(PRESENT(languages))
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
