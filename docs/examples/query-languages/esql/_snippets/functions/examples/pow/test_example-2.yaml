# Source: query-languages/esql/_snippets/functions/examples/pow.md
--- !metadata
id: example-2
language: esql
topic: pow
subtopic: default
example_order: 2
--- !code
|
  ROW base = 4, exponent = 0.5
  | EVAL s = POW(base, exponent)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
