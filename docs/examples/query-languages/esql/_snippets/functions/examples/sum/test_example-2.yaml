# Source: query-languages/esql/_snippets/functions/examples/sum.md
--- !metadata
id: example-2
language: esql
topic: sum
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS total_salary_changes = SUM(MV_MAX(salary_change))
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
