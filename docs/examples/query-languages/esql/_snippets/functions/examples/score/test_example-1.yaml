# Source: query-languages/esql/_snippets/functions/examples/score.md
--- !metadata
id: example-1
language: esql
topic: score
subtopic: default
example_order: 1
--- !code
|
  FROM books METADATA _score
  | WHERE match(title, "Return") AND match(author, "Tolkien")
  | EVAL first_score = score(match(title, "Return"))
--- !result

--- !test
stage: books
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
