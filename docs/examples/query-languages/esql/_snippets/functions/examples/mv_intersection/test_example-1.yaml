# Source: query-languages/esql/_snippets/functions/examples/mv_intersection.md
--- !metadata
id: example-1
language: esql
topic: mv_intersection
subtopic: default
example_order: 1
--- !code
|
  ROW a = [1, 2, 3, 4, 5], b = [2, 3, 4, 5, 6]
  | EVAL finalValue = MV_INTERSECTION(a, b)
  | KEEP finalValue
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
