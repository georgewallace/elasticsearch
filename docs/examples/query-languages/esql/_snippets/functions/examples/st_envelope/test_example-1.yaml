# Source: query-languages/esql/_snippets/functions/examples/st_envelope.md
--- !metadata
id: example-1
language: esql
topic: st_envelope
subtopic: default
example_order: 1
--- !code
|
  FROM airport_city_boundaries
  | WHERE abbrev == "CPH"
  | EVAL envelope = ST_ENVELOPE(city_boundary)
  | KEEP abbrev, airport, envelope
--- !result

--- !test
stage: airports
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
