# Source: query-languages/esql/_snippets/functions/examples/mv_sort.md
--- !metadata
id: example-1
language: esql
topic: mv_sort
subtopic: default
example_order: 1
--- !code
|
  ROW a = [4, 2, -3, 2]
  | EVAL sa = mv_sort(a), sd = mv_sort(a, "DESC")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
