# Source: query-languages/esql/_snippets/functions/examples/min.md
--- !metadata
id: example-2
language: esql
topic: min
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS min_avg_salary_change = MIN(MV_AVG(salary_change))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
