# Source: query-languages/esql/_snippets/functions/examples/st_distance.md
--- !metadata
id: example-1
language: esql
topic: st_distance
subtopic: default
example_order: 1
--- !code
|
  FROM airports
  | WHERE abbrev == "CPH"
  | EVAL distance = ST_DISTANCE(location, city_location)
  | KEEP abbrev, name, location, city_location, distance
--- !result

--- !test
stage: airports
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
