# Source: query-languages/esql/_snippets/functions/examples/rate.md
--- !metadata
id: example-1
language: esql
topic: rate
subtopic: default
example_order: 1
--- !code
|
  TS k8s
  | STATS max_rate=MAX(RATE(network.total_bytes_in)) BY time_bucket = TBUCKET(5minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
