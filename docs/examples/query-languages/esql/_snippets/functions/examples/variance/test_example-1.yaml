# Source: query-languages/esql/_snippets/functions/examples/variance.md
--- !metadata
id: example-1
language: esql
topic: variance
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS var_height = VARIANCE(height)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
