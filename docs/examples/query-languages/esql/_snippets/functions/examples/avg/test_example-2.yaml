# Source: query-languages/esql/_snippets/functions/examples/avg.md
--- !metadata
id: example-2
language: esql
topic: avg
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS avg_salary_change = ROUND(AVG(MV_AVG(salary_change)), 10)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
