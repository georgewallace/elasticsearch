# Source: query-languages/esql/_snippets/functions/examples/st_xmin.md
--- !metadata
id: example-1
language: esql
topic: st_xmin
subtopic: default
example_order: 1
--- !code
|
  FROM airport_city_boundaries
  | WHERE abbrev == "CPH"
  | EVAL envelope = ST_ENVELOPE(city_boundary)
  | EVAL xmin = ST_XMIN(envelope), xmax = ST_XMAX(envelope), ymin = ST_YMIN(envelope), ymax = ST_YMAX(envelope)
  | KEEP abbrev, airport, xmin, xmax, ymin, ymax
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
