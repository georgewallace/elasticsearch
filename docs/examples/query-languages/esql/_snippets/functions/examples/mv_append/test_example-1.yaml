# Source: query-languages/esql/_snippets/functions/examples/mv_append.md
--- !metadata
id: example-1
language: esql
topic: mv_append
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | WHERE emp_no == 10039 OR emp_no == 10040
  | SORT emp_no
  | EVAL dates = MV_APPEND(birth_date, hire_date)
  | KEEP emp_no, birth_date, hire_date, dates
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
