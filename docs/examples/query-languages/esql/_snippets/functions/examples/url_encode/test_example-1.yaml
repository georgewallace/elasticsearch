# Source: query-languages/esql/_snippets/functions/examples/url_encode.md
--- !metadata
id: example-1
language: esql
topic: url_encode
subtopic: default
example_order: 1
--- !code
|
  ROW u = "https://example.com/?x=foo bar&y=baz" | EVAL u = URL_ENCODE(u)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
