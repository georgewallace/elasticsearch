# Source: query-languages/esql/_snippets/functions/examples/to_ip.md
--- !metadata
id: example-2
language: esql
topic: to_ip
subtopic: default
example_order: 2
--- !code
|
  ROW s = "1.1.010.1" | EVAL ip = TO_IP(s, {"leading_zeros":"octal"})
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
