# Source: query-languages/esql/_snippets/functions/examples/first.md
--- !metadata
id: example-1
language: esql
topic: first
subtopic: default
example_order: 1
--- !code
|
  FROM k8s
  | STATS first_bytes_in = FIRST(network.bytes_in, @timestamp) BY pod
  | SORT pod ASC
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
