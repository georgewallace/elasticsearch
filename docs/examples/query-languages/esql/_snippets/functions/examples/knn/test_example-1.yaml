# Source: query-languages/esql/_snippets/functions/examples/knn.md
--- !metadata
id: example-1
language: esql
topic: knn
subtopic: default
example_order: 1
--- !code
|
  from colors metadata _score
  | where knn(rgb_vector, [0, 120, 0])
  | sort _score desc, color asc
--- !result

--- !test
stage: colors
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
