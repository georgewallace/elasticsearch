# Source: query-languages/esql/_snippets/functions/examples/replace.md
--- !metadata
id: example-1
language: esql
topic: replace
subtopic: default
example_order: 1
--- !code
|
  ROW str = "Hello World"
  | EVAL str = REPLACE(str, "World", "Universe")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
