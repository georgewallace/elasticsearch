# Source: query-languages/esql/_snippets/functions/examples/percentile.md
--- !metadata
id: example-2
language: esql
topic: percentile
subtopic: default
example_order: 2
--- !code
|
  FROM employees
  | STATS p80_max_salary_change = PERCENTILE(MV_MAX(salary_change), 80)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
