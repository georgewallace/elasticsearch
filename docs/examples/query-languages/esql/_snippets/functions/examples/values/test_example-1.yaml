# Source: query-languages/esql/_snippets/functions/examples/values.md
--- !metadata
id: example-1
language: esql
topic: values
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | EVAL first_letter = SUBSTRING(first_name, 0, 1)
  | STATS first_name = MV_SORT(VALUES(first_name)) BY first_letter
  | SORT first_letter
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
