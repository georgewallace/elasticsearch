# Source: query-languages/esql/_snippets/functions/examples/mv_max.md
--- !metadata
id: example-2
language: esql
topic: mv_max
subtopic: default
example_order: 2
--- !code
|
  ROW a=["foo", "zoo", "bar"]
  | EVAL max_a = MV_MAX(a)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
