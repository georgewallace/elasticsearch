# Source: query-languages/esql/_snippets/functions/examples/absent.md
--- !metadata
id: example-3
language: esql
topic: absent
subtopic: default
example_order: 3
--- !code
|
  FROM employees
  | WHERE emp_no == 10020
  | STATS is_absent = TO_INTEGER(ABSENT(languages))
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
