# Source: query-languages/esql/_snippets/functions/examples/mv_contains.md
--- !metadata
id: example-3
language: esql
topic: mv_contains
subtopic: default
example_order: 3
--- !code
|
  FROM airports
  | WHERE mv_contains(type, ["major","military"]) AND scalerank == 9
  | KEEP scalerank, name, country
--- !result

--- !test
stage: airports
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
