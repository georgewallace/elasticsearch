# Source: query-languages/esql/_snippets/functions/examples/length.md
--- !metadata
id: example-1
language: esql
topic: length
subtopic: default
example_order: 1
--- !code
|
  FROM airports
  | WHERE country == "India"
  | KEEP city
  | EVAL fn_length = LENGTH(city)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
