# Source: query-languages/esql/_snippets/functions/examples/bucket.md
--- !metadata
id: example-7
language: esql
topic: bucket
subtopic: default
example_order: 7
--- !code
|
  FROM sample_data
  | WHERE @timestamp >= NOW() - 1 day and @timestamp < NOW()
  | STATS COUNT(*) BY bucket = BUCKET(@timestamp, 25, NOW() - 1 day, NOW())
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
