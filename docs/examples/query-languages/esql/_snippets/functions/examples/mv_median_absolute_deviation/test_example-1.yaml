# Source: query-languages/esql/_snippets/functions/examples/mv_median_absolute_deviation.md
--- !metadata
id: example-1
language: esql
topic: mv_median_absolute_deviation
subtopic: default
example_order: 1
--- !code
|
  ROW values = [0, 2, 5, 6]
  | EVAL median_absolute_deviation = MV_MEDIAN_ABSOLUTE_DEVIATION(values), median = MV_MEDIAN(values)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
