# Source: query-languages/esql/_snippets/functions/examples/case.md
--- !metadata
id: example-2
language: esql
topic: case
subtopic: default
example_order: 2
--- !code
|
  FROM sample_data
  | EVAL successful = CASE(
      STARTS_WITH(message, "Connected to"), 1,
      message == "Connection error", 0
    )
  | STATS success_rate = AVG(successful)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
