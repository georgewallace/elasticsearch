# Source: query-languages/esql/_snippets/functions/examples/count_distinct_over_time.md
--- !metadata
id: example-1
language: esql
topic: count_distinct_over_time
subtopic: default
example_order: 1
--- !code
|
  TS k8s
  | STATS distincts=COUNT_DISTINCT(COUNT_DISTINCT_OVER_TIME(network.cost)),
          distincts_imprecise=COUNT_DISTINCT(COUNT_DISTINCT_OVER_TIME(network.cost, 100))
    BY cluster, time_bucket = TBUCKET(1minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
