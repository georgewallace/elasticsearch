# Source: query-languages/esql/_snippets/commands/examples/docs-lookup-join.csv-spec/lookupJoinServiceId.md
--- !metadata
id: example-1
language: esql
topic: lookupJoinServiceId
subtopic: default
example_order: 1
--- !code
|
  FROM app_logs
  | LOOKUP JOIN service_owners ON service_id
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
