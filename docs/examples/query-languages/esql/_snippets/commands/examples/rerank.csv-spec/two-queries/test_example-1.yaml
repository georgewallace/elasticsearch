# Source: query-languages/esql/_snippets/commands/examples/rerank.csv-spec/two-queries.md
--- !metadata
id: example-1
language: esql
topic: two-queries
subtopic: default
example_order: 1
--- !code
|
  FROM books METADATA _score
  | WHERE MATCH(description, "hobbit") OR MATCH(author, "Tolkien")
  | SORT _score DESC
  | LIMIT 100
  | RERANK rerank_score = "hobbit" ON description, author WITH { "inference_id" : "test_reranker" }
  | SORT rerank_score
  | LIMIT 3
  | KEEP title, _score, rerank_score
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
