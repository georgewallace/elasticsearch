# Source: query-languages/esql/_snippets/commands/examples/rerank.csv-spec/simple-query.md
--- !metadata
id: example-1
language: esql
topic: simple-query
subtopic: default
example_order: 1
--- !code
|
  FROM books METADATA _score
  | WHERE MATCH(description, "hobbit")
  | SORT _score DESC
  | LIMIT 100
  | RERANK "hobbit" ON description WITH { "inference_id" : "test_reranker" }
  | LIMIT 3
  | KEEP title, _score
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
