# Source: query-languages/esql/_snippets/commands/examples/lookup-join.csv-spec/filterOnRightSide.md
--- !metadata
id: example-1
language: esql
topic: filterOnRightSide
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | EVAL language_code = languages
  | LOOKUP JOIN languages_lookup ON language_code
  | WHERE emp_no >= 10091 AND emp_no < 10094
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
