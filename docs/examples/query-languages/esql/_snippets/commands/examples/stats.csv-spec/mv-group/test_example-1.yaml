# Source: query-languages/esql/_snippets/commands/examples/stats.csv-spec/mv-group.md
--- !metadata
id: example-1
language: esql
topic: mv-group
subtopic: default
example_order: 1
--- !code
|
  ROW price = 10, color = ["blue", "pink", "yellow"]
  | STATS SUM(price) BY color
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
