# Source: query-languages/esql/_snippets/commands/examples/stats.csv-spec/docsStatsByExpression.md
--- !metadata
id: example-1
language: esql
topic: docsStatsByExpression
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS my_count = COUNT() BY LEFT(last_name, 1)
  | SORT `LEFT(last_name, 1)`
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
