# Source: query-languages/esql/_snippets/commands/examples/stats.csv-spec/stats.md
--- !metadata
id: example-1
language: esql
topic: stats
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS count = COUNT(emp_no) BY languages
  | SORT languages
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
