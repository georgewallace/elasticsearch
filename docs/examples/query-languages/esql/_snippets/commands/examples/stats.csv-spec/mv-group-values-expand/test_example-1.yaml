# Source: query-languages/esql/_snippets/commands/examples/stats.csv-spec/mv-group-values-expand.md
--- !metadata
id: example-1
language: esql
topic: mv-group-values-expand
subtopic: default
example_order: 1
--- !code
|
  ROW color = ["blue", "pink", "yellow"]
  | MV_EXPAND color
  | STATS VALUES(color) BY color
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
