# Source: query-languages/esql/_snippets/commands/examples/stats.csv-spec/docsStatsAvgNestedExpression.md
--- !metadata
id: example-1
language: esql
topic: docsStatsAvgNestedExpression
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS avg_salary_change = ROUND(AVG(MV_AVG(salary_change)), 10)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
