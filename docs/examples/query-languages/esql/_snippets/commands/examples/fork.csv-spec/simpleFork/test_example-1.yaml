# Source: query-languages/esql/_snippets/commands/examples/fork.csv-spec/simpleFork.md
--- !metadata
id: example-1
language: esql
topic: simpleFork
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | FORK ( WHERE emp_no == 10001 )
         ( WHERE emp_no == 10002 )
  | KEEP emp_no, _fork
  | SORT emp_no
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
