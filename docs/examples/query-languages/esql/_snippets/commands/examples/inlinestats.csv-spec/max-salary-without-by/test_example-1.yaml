# Source: query-languages/esql/_snippets/commands/examples/inlinestats.csv-spec/max-salary-without-by.md
--- !metadata
id: example-1
language: esql
topic: max-salary-without-by
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP emp_no, languages, salary
  | INLINE STATS max_salary = MAX(salary)
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
