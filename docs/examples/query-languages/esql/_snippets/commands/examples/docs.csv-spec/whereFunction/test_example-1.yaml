# Source: query-languages/esql/_snippets/commands/examples/docs.csv-spec/whereFunction.md
--- !metadata
id: example-1
language: esql
topic: whereFunction
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP first_name, last_name, height
  | WHERE LENGTH(first_name) < 4
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
