# Source: query-languages/esql/_snippets/commands/examples/eval.csv-spec/eval.md
--- !metadata
id: example-1
language: esql
topic: eval
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | SORT emp_no
  | KEEP first_name, last_name, height
  | EVAL height_feet = height * 3.281, height_cm = height * 100
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
