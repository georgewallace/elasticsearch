# Source: query-languages/esql/_snippets/commands/examples/eval.csv-spec/evalUnnamedColumn.md
--- !metadata
id: example-1
language: esql
topic: evalUnnamedColumn
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | SORT emp_no
  | KEEP first_name, last_name, height
  | EVAL height * 3.281
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
