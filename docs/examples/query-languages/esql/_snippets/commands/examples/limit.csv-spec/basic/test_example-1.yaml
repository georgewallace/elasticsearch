# Source: query-languages/esql/_snippets/commands/examples/limit.csv-spec/basic.md
--- !metadata
id: example-1
language: esql
topic: basic
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | SORT emp_no ASC
  | LIMIT 5
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
