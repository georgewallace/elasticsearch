# Source: query-languages/esql/_snippets/commands/layout/enrich.md
--- !metadata
id: example-1
language: esql
topic: enrich
subtopic: default
example_order: 1
--- !code
|
  ENRICH policy [ON match_field] [WITH [new_name1 = ]field1, [new_name2 = ]field2, ...]
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
