# Source: query-languages/esql/_snippets/commands/layout/fork.md
--- !metadata
id: example-1
language: esql
topic: fork
subtopic: default
example_order: 1
--- !code
|
  FORK ( <processing_commands> ) ( <processing_commands> ) ... ( <processing_commands> )
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
