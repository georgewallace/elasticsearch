# Source: query-languages/esql/_snippets/commands/layout/lookup-join.md
--- !metadata
id: example-1
language: esql
topic: lookup-join
subtopic: default
example_order: 1
--- !code
|
  FROM <source_index>
  | LOOKUP JOIN <lookup_index> ON <join_condition>
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
