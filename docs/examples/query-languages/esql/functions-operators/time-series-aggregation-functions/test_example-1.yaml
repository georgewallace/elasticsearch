# Source: query-languages/esql/functions-operators/time-series-aggregation-functions.md
--- !metadata
id: example-1
language: esql
topic: time-series-aggregation-functions
subtopic: default
example_order: 1
--- !code
|
  TS metrics
    | WHERE TRANGE(1h)
    | STATS AVG(RATE(requests, 10m)) BY TBUCKET(1m), host
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
