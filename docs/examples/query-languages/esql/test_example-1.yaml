# Source: query-languages/esql.md
--- !metadata
id: example-1
language: esql
topic: esql
subtopic: default
example_order: 1
--- !code
|
  FROM sample_data
  | SORT @timestamp DESC
  | LIMIT 3
--- !result

--- !test
stage: sample_data
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
