# Source: query-languages/esql/kibana/docs/functions/categorize.md
--- !metadata
id: example-1
language: esql
topic: categorize
subtopic: default
example_order: 1
--- !code
|
  FROM sample_data
  | STATS count=COUNT() BY category=CATEGORIZE(message)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
