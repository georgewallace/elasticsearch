# Source: query-languages/esql/kibana/docs/functions/to_geotile.md
--- !metadata
id: example-1
language: esql
topic: to_geotile
subtopic: default
example_order: 1
--- !code
|
  ROW string = "4/8/5"
  | EVAL geotile = TO_GEOTILE(string)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
