# Source: query-languages/esql/kibana/docs/functions/st_centroid_agg.md
--- !metadata
id: example-1
language: esql
topic: st_centroid_agg
subtopic: default
example_order: 1
--- !code
|
  FROM airports
  | STATS centroid=ST_CENTROID_AGG(location)
--- !result

--- !test
stage: airports
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
