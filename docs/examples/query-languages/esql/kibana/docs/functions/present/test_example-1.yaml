# Source: query-languages/esql/kibana/docs/functions/present.md
--- !metadata
id: example-1
language: esql
topic: present
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS is_present = PRESENT(languages)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
