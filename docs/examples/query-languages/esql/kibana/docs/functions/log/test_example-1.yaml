# Source: query-languages/esql/kibana/docs/functions/log.md
--- !metadata
id: example-1
language: esql
topic: log
subtopic: default
example_order: 1
--- !code
|
  ROW base = 2.0, value = 8.0
  | EVAL s = LOG(base, value)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
