# Source: query-languages/esql/kibana/docs/functions/to_dense_vector.md
--- !metadata
id: example-1
language: esql
topic: to_dense_vector
subtopic: default
example_order: 1
--- !code
|
  row ints = [1, 2, 3]
  | eval vector = to_dense_vector(ints)
  | keep vector
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
