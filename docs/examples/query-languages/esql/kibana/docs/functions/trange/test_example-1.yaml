# Source: query-languages/esql/kibana/docs/functions/trange.md
--- !metadata
id: example-1
language: esql
topic: trange
subtopic: default
example_order: 1
--- !code
|
  FROM k8s
  | WHERE TRANGE(1h)
  | KEEP @timestamp
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
