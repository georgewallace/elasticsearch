# Source: query-languages/esql/kibana/docs/functions/date_format.md
--- !metadata
id: example-1
language: esql
topic: date_format
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP first_name, last_name, hire_date
  | EVAL hired = DATE_FORMAT("yyyy-MM-dd", hire_date)
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
