# Source: query-languages/esql/kibana/docs/functions/median_absolute_deviation.md
--- !metadata
id: example-1
language: esql
topic: median_absolute_deviation
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | STATS MEDIAN(salary), MEDIAN_ABSOLUTE_DEVIATION(salary)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
