# Source: query-languages/esql/kibana/docs/functions/count_distinct.md
--- !metadata
id: example-1
language: esql
topic: count_distinct
subtopic: default
example_order: 1
--- !code
|
  FROM hosts
  | STATS COUNT_DISTINCT(ip0), COUNT_DISTINCT(ip1)
--- !result

--- !test
stage: hosts
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
