# Source: query-languages/esql/kibana/docs/functions/ends_with.md
--- !metadata
id: example-1
language: esql
topic: ends_with
subtopic: default
example_order: 1
--- !code
|
  FROM employees
  | KEEP last_name
  | EVAL ln_E = ENDS_WITH(last_name, "d")
--- !result

--- !test
stage: employees
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
