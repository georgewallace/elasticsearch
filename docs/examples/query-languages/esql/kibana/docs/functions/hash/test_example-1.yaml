# Source: query-languages/esql/kibana/docs/functions/hash.md
--- !metadata
id: example-1
language: esql
topic: hash
subtopic: default
example_order: 1
--- !code
|
  FROM sample_data
  | WHERE message != "Connection error"
  | EVAL md5 = hash("md5", message), sha256 = hash("sha256", message)
  | KEEP message, md5, sha256
--- !result

--- !test
stage: sample_data
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
