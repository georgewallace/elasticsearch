# Source: query-languages/esql/kibana/docs/functions/v_magnitude.md
--- !metadata
id: example-1
language: esql
topic: v_magnitude
subtopic: default
example_order: 1
--- !code
|
  from colors
   | eval magnitude = v_magnitude(rgb_vector)
   | sort magnitude desc, color asc
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
