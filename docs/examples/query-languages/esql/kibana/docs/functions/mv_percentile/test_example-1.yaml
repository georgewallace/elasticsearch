# Source: query-languages/esql/kibana/docs/functions/mv_percentile.md
--- !metadata
id: example-1
language: esql
topic: mv_percentile
subtopic: default
example_order: 1
--- !code
|
  ROW values = [5, 5, 10, 12, 5000]
  | EVAL p50 = MV_PERCENTILE(values, 50), median = MV_MEDIAN(values)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
