# Source: query-languages/esql/kibana/docs/functions/text_embedding.md
--- !metadata
id: example-1
language: esql
topic: text_embedding
subtopic: default
example_order: 1
--- !code
|
  FROM dense_vector_text METADATA _score
  | WHERE KNN(text_embedding_field, TEXT_EMBEDDING("be excellent to each other", "test_dense_inference"))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
