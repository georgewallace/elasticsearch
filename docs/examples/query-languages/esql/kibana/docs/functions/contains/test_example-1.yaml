# Source: query-languages/esql/kibana/docs/functions/contains.md
--- !metadata
id: example-1
language: esql
topic: contains
subtopic: default
example_order: 1
--- !code
|
  ROW a = "hello"
  | EVAL has_ll = CONTAINS(a, "ll")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
