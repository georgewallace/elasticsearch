# Source: query-languages/esql/kibana/docs/functions/deriv.md
--- !metadata
id: example-1
language: esql
topic: deriv
subtopic: default
example_order: 1
--- !code
|
  TS datenanos-k8s
  | WHERE pod == "three"
  | STATS max_deriv = MAX(DERIV(network.cost)) BY time_bucket = BUCKET(@timestamp,5minute), pod
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
