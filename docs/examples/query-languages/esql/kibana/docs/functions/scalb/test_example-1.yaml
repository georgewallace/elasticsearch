# Source: query-languages/esql/kibana/docs/functions/scalb.md
--- !metadata
id: example-1
language: esql
topic: scalb
subtopic: default
example_order: 1
--- !code
|
  row x = 3.0, y = 10 | eval z = scalb(x, y)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
