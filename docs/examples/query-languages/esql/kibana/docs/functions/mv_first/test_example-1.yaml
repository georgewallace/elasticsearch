# Source: query-languages/esql/kibana/docs/functions/mv_first.md
--- !metadata
id: example-1
language: esql
topic: mv_first
subtopic: default
example_order: 1
--- !code
|
  ROW a="foo;bar;baz"
  | EVAL first_a = MV_FIRST(SPLIT(a, ";"))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
