# Source: query-languages/esql/kibana/docs/functions/to_base64.md
--- !metadata
id: example-1
language: esql
topic: to_base64
subtopic: default
example_order: 1
--- !code
|
  ROW a = "elastic"
  | EVAL e = TO_BASE64(a)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
