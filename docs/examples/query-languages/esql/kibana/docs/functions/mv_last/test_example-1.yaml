# Source: query-languages/esql/kibana/docs/functions/mv_last.md
--- !metadata
id: example-1
language: esql
topic: mv_last
subtopic: default
example_order: 1
--- !code
|
  ROW a="foo;bar;baz"
  | EVAL last_a = MV_LAST(SPLIT(a, ";"))
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
