# Source: query-languages/esql/kibana/docs/functions/match_phrase.md
--- !metadata
id: example-1
language: esql
topic: match_phrase
subtopic: default
example_order: 1
--- !code
|
  FROM books
  | WHERE MATCH_PHRASE(author, "William Faulkner")
--- !result

--- !test
stage: books
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
