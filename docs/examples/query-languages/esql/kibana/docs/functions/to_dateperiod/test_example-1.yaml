# Source: query-languages/esql/kibana/docs/functions/to_dateperiod.md
--- !metadata
id: example-1
language: esql
topic: to_dateperiod
subtopic: default
example_order: 1
--- !code
|
  ROW x = "2024-01-01"::datetime
  | EVAL y = x + "3 DAYS"::date_period, z = x - TO_DATEPERIOD("3 days");
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
