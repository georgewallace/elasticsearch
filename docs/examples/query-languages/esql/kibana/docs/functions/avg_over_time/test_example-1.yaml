# Source: query-languages/esql/kibana/docs/functions/avg_over_time.md
--- !metadata
id: example-1
language: esql
topic: avg_over_time
subtopic: default
example_order: 1
--- !code
|
  TS k8s
  | STATS max_cost=MAX(AVG_OVER_TIME(network.cost)) BY cluster, time_bucket = TBUCKET(1minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
