# Source: query-languages/esql/kibana/docs/functions/tbucket.md
--- !metadata
id: example-1
language: esql
topic: tbucket
subtopic: default
example_order: 1
--- !code
|
  FROM sample_data
  | STATS min = MIN(@timestamp), max = MAX(@timestamp) BY bucket = TBUCKET(1 hour)
  | SORT min
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
