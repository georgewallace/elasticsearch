# Source: query-languages/esql/kibana/docs/functions/irate.md
--- !metadata
id: example-1
language: esql
topic: irate
subtopic: default
example_order: 1
--- !code
|
  TS k8s | WHERE pod == "one"
  | STATS irate_bytes_in = SUM(IRATE(network.total_bytes_in)) BY cluster, time_bucket = TBUCKET(10minute)
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
