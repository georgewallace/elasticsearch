# Source: query-languages/esql/kibana/docs/functions/to_version.md
--- !metadata
id: example-1
language: esql
topic: to_version
subtopic: default
example_order: 1
--- !code
|
  ROW v = TO_VERSION("1.2.3")
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
