# Source: query-languages/esql/kibana/docs/functions/v_l1_norm.md
--- !metadata
id: example-1
language: esql
topic: v_l1_norm
subtopic: default
example_order: 1
--- !code
|
  from colors
  | eval similarity = v_l1_norm(rgb_vector, [0, 255, 255])
  | sort similarity desc, color asc
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
