# Source: query-languages/esql/esql-syntax.md
--- !metadata
id: example-4
language: esql
topic: esql-syntax
subtopic: default
example_order: 4
--- !code
|
  FROM index
  | STATS COUNT(`1.field`)
  | EVAL my_count = `COUNT(``1.field``)`
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
