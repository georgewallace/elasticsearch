# Source: query-languages/esql/esql-syntax.md
--- !metadata
id: example-8
language: esql
topic: esql-syntax
subtopic: default
example_order: 8
--- !code
|
  // Query the employees index
  FROM employees
  | WHERE height > 2
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
