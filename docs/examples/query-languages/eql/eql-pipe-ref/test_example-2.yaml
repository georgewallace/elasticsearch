# Source: query-languages/eql/eql-pipe-ref.md
--- !metadata
id: example-2
language: eql
topic: eql-pipe-ref
subtopic: default
example_order: 2
--- !code
|
  process where process.name == "svchost.exe"
  | tail 5
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
