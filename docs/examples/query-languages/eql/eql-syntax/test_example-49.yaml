# Source: query-languages/eql/eql-syntax.md
--- !metadata
id: example-49
language: eql
topic: eql-syntax
subtopic: default
example_order: 49
--- !code
|
  sample by host
    [ file where file.extension == "exe" ]
    [ process where true ]
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
