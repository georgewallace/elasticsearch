# Source: query-languages/eql/eql-syntax.md
--- !metadata
id: example-50
language: eql
topic: eql-syntax
subtopic: default
example_order: 50
--- !code
|
  stringContains(process.name,".exe")  // Matches ".exe" but not ".EXE" or ".Exe"
  stringContains~(process.name,".exe") // Matches ".exe", ".EXE", or ".Exe"
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
