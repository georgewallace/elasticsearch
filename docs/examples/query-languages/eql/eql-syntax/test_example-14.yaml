# Source: query-languages/eql/eql-syntax.md
--- !metadata
id: example-14
language: eql
topic: eql-syntax
subtopic: default
example_order: 14
--- !code
|
  process where ( 4 / process.args_count ) == 1
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
