# Source: query-languages/sql/sql-security.md
--- !metadata
id: example-1
language: elastic_console
topic: sql-security
subtopic: default
example_order: 1
--- !code
|
  POST /_security/role/cli_or_drivers_minimal
  {
    "cluster": ["cluster:monitor/main"],
    "indices": [
      {
        "names": ["test"],
        "privileges": ["read", "indices:admin/get"]
      }
    ]
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
