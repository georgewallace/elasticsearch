# Source: query-languages/sql/sql-functions-aggs.md
--- !metadata
id: example-19
language: sql
topic: sql-functions-aggs
subtopic: default
example_order: 19
--- !code
|
  SELECT gender, FIRST_VALUE(first_name, birth_date) FROM emp GROUP BY gender ORDER BY gender;

      gender    |   FIRST_VALUE(first_name, birth_date)
  --------------+--------------------------------------
  null          |   Lillian
  F             |   Sumant
  M             |   Remzi
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
