# Source: query-languages/sql/sql-rest-overview.md
--- !metadata
id: example-1
language: elastic_console
topic: sql-rest-overview
subtopic: default
example_order: 1
--- !code
|
  POST /_sql?format=txt
  {
    "query": "SELECT * FROM library ORDER BY page_count DESC LIMIT 5"
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
