# Source: query-languages/sql/sql-rest-filtering.md
--- !metadata
id: example-2
language: elastic_console
topic: sql-rest-filtering
subtopic: default
example_order: 2
--- !code
|
  POST /_sql?format=txt
  {
    "query": "SELECT * FROM library",
    "filter": {
      "terms": {
        "_routing": ["abc"]
      }
    }
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
