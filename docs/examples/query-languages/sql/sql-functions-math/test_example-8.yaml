# Source: query-languages/sql/sql-functions-math.md
--- !metadata
id: example-8
language: sql
topic: sql-functions-math
subtopic: default
example_order: 8
--- !code
|
  SELECT E(), CEIL(E());

         E()       |   CEIL(E())
  -----------------+---------------
  2.718281828459045|3
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
