# Source: query-languages/sql/sql-syntax-show-tables.md
--- !metadata
id: example-3
language: sql
topic: sql-syntax-show-tables
subtopic: default
example_order: 3
--- !code
|
  SHOW TABLES "*,-l*";

   catalog       |     name      | type     |     kind
  ---------------+---------------+----------+---------------
  javaRestTest      |emp            |TABLE     |INDEX
  javaRestTest      |employees      |VIEW      |ALIAS
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
