# Source: query-languages/sql/sql-functions-conditional.md
--- !metadata
id: example-5
language: sql
topic: sql-functions-conditional
subtopic: default
example_order: 5
--- !code
|
  CASE expression
       WHEN value1 THEN result1
      [WHEN value2 THEN result2]
      [WHEN ...]
      [ELSE default_result]
  END
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
