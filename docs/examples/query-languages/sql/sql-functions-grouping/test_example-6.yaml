# Source: query-languages/sql/sql-functions-grouping.md
--- !metadata
id: example-6
language: sql
topic: sql-functions-grouping
subtopic: default
example_order: 6
--- !code
|
  SELECT MONTH(HISTOGRAM(birth_date), 2)) AS h, COUNT(*) as c FROM emp GROUP BY h ORDER BY h DESC;
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
