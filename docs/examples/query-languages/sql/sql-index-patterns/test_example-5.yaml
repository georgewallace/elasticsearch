# Source: query-languages/sql/sql-index-patterns.md
--- !metadata
id: example-5
language: sql
topic: sql-index-patterns
subtopic: default
example_order: 5
--- !code
|
  SHOW TABLES LIKE 'emp!%' ESCAPE '!';

   catalog       |     name      |     type      |     kind
  ---------------+---------------+---------------+---------------
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
