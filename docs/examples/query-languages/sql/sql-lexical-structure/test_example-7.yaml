# Source: query-languages/sql/sql-lexical-structure.md
--- !metadata
id: example-7
language: sql
topic: sql-lexical-structure
subtopic: default
example_order: 7
--- !code
|
  SELECT "first_name" 
    FROM "musicians"  
   WHERE "last_name"  
       = 'Carroll'
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
