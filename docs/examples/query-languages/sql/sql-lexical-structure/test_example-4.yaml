# Source: query-languages/sql/sql-lexical-structure.md
--- !metadata
id: example-4
language: sql
topic: sql-lexical-structure
subtopic: default
example_order: 4
--- !code
|
  SELECT "from" FROM "<logstash-{now/d}>"
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
