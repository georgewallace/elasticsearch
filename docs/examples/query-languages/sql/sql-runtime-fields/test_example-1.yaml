# Source: query-languages/sql/sql-runtime-fields.md
--- !metadata
id: example-1
language: elastic_console
topic: sql-runtime-fields
subtopic: default
example_order: 1
--- !code
|
  POST _sql?format=txt
  {
    "runtime_mappings": {
      "release_day_of_week": {
        "type": "keyword",
        "script": """
          emit(doc['release_date'].value.dayOfWeekEnum.toString())
        """
      }
    },
    "query": """
      SELECT * FROM library WHERE page_count > 300 AND author = 'Frank Herbert'
    """
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
