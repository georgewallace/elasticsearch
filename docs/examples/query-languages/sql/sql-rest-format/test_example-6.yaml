# Source: query-languages/sql/sql-rest-format.md
--- !metadata
id: example-6
language: elastic_console
topic: sql-rest-format
subtopic: default
example_order: 6
--- !code
|
  POST /_sql?format=yaml
  {
    "query": "SELECT * FROM library ORDER BY page_count DESC",
    "fetch_size": 5
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
