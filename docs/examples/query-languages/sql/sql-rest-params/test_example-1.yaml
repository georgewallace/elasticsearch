# Source: query-languages/sql/sql-rest-params.md
--- !metadata
id: example-1
language: elastic_console
topic: sql-rest-params
subtopic: default
example_order: 1
--- !code
|
  POST /_sql?format=txt
  {
    "query": "SELECT YEAR(release_date) AS year FROM library WHERE page_count > 300 AND author = 'Frank Herbert' GROUP BY year HAVING COUNT(*) > 0"
  }
--- !result

--- !test
steps:
  - action:
      kind: EXAMPLE_CODE
  - expected:
      kind: EXAMPLE_RESULT
      status_codes: [200, 201]
      match_fields:
        - field: '*'
          match_kind: EQUALS
      ignore_fields:
        - took
